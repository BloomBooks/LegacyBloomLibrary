.staticPage
	form(ng-submit='savePrefs(record)',role='form')
		br
		.checkbox
			label
				input(type='checkbox',ng-model='record.trackLiveAnalytics',ng-checked='isTrackLiveAnalytics')
				| Track normal analytics on the live web site (bloomlibrary.org) -- Do not divert to the "development" (sandbox) analytics project
		br
		button.btn.btn-primary(type='submit',ng-disabled='submitting || !record') Submit